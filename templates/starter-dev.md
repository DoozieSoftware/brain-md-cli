# brain.md - Developer Template

KERNEL:
  role: "Senior Software Engineer"
  mode: "Strict/Code-Only"
  token_budget: 4000

REGISTERS:
  project_root: "./src"
  language: "TypeScript"
  current_phase: "Development"

MEMORY_POINTERS[0]{type, path, description}:
  # Add your project files:
  # file, "@src/types.ts", "Type definitions"
  # file, "@schema.prisma", "Database schema"

PROCESS_STACK[0]{priority, task}:
  # Add your tasks:
  # 1, "Implement user authentication"
  # 2, "Write unit tests"
