# context.md - Live Session State
# Generated from brain.md by 'brain boot' command
# Created: 2026-01-13 23:40:40
#
# This is your live RAM - edit freely during the session.
# Changes are auto-compiled when using 'brain watch'.
#
# === KERNEL (AI Configuration) ===
KERNEL:
  user: "Marcus, Lead Architect"
  role: "Senior Legacy Migration Engineer"
  task: "Map legacy configuration keys to new microservice environment variables."

# === REGISTERS (Session Variables) ===
REGISTERS:
  strict_mode: "TRUE"
  target_lang: "Go 1.21"
  forbidden_types: "float32, float64"
  env: "production"

# === MEMORY_POINTERS (File Context) ===
MEMORY_POINTERS[2]:
"@global_config_map.json", "Legacy Config Mapping"
"@deployment_env.yaml", "Production Environment"

# === PROCESS_STACK (Current Workflow) ===
PROCESS_STACK[2]:
"Analyze Config Map keys", "Identify matching env vars"
"Generate .env file", "Use production defaults"

# === SESSION_SCRATCHPAD (Live Notes) ===
SESSION_SCRATCHPAD:
- Warning: The COBOL code uses `ROUNDED`.
- Note: We paused the transpilation.
- Priority: We need to figure out what `CONF_001` means in the new system.
